<?php if (!$this) { exit(header('HTTP/1.0 403 Forbidden')); } ?>

<div class="container">
    <p>
		Esta é a página de erro. Será mostrado quando uma página (= controller / method) não existir.
	</p>
</div>
